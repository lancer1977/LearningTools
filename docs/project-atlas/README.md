# Project Atlas

`LearningTools` contains spelling-test learning apps across Blazor/WASM/MAUI. The current CI lane exercises the existing test project while platform workloads are normalized.

## Validation

- `bash scripts/validate.sh`
