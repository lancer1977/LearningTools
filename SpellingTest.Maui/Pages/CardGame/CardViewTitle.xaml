<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             MinimumHeightRequest="80"
             x:Class="SpellingTest.Maui.Pages.CardGame.CardViewTitle">
    <ContentView.Content>
        <StackLayout>
            <Image BackgroundColor="Cyan" VerticalOptions="Center"   Source="{Binding Image}"  IsVisible="{Binding ShowImage}" />

            <Label Text="{Binding Name}" HorizontalTextAlignment="Center" IsVisible="{Binding ShowName}"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>